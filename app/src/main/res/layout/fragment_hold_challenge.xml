<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="@dimen/spacing_l"
    android:background="@drawable/bg_app_gradient">

    <FrameLayout
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:layout_marginTop="32dp"
        android:background="@drawable/bg_challenge_icon_circle">

        <ImageView
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_challenge_hold"
app:tint="?attr/colorPrimary" />
        </FrameLayout>

        <TextView
            android:id="@+id/tvHoldTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_m"
            android:text="@string/hold_title"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="?attr/colorPrimary" />

    <TextView
        android:id="@+id/tvHoldInstruction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="@string/hold_instruction"
        android:textSize="14sp"
        android:textColor="@color/lilac_on_surface_secondary" />

    <!-- Hold button with circular progress -->
    <FrameLayout
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginTop="48dp">

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/holdProgress"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_gravity="center"
            android:indeterminate="false"
            app:indicatorSize="200dp"
            app:trackThickness="8dp"
            app:indicatorColor="@color/purple_accent"
            app:trackColor="@color/challenge_icon_bg"
            app:trackCornerRadius="4dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnHold"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_gravity="center"
            android:text="@string/hold_button"
            android:textAllCaps="false"
            android:textSize="18sp"
            app:cornerRadius="75dp"
            app:backgroundTint="@color/challenge_button_bg"
            android:textColor="@color/white"
            app:elevation="8dp" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvHoldTimer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_l"
        android:textSize="16sp"
        android:textColor="@color/purple_medium"
        android:text="0:00 / 0:10" />

</LinearLayout>
